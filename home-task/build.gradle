plugins {
    id 'java'
}

jar {
   baseName = 'hello-gradle'
    manifest {
        attributes(
                'Main-Class': 'com.test.Project'
        )
    }
}
 repositories {
    jcenter()
    mavenCentral()
}

 dependencies {
    compile group: 'org.apache.commons', name: 'commons-collections4', version: '4.0'
    compile group: 'log4j', name: 'log4j', version: '1.2.17'
    testCompile group: 'junit', name: 'junit', version: '4.+'
}

task "build-custom" {
    doLast {
        println 'gradle file is builded'
    }
}
task "test-custom"(dependsOn: "build-custom") {
    doLast {
        println 'Testing gradle file OK'
    }
}
task "hello"(dependsOn: "test-custom") {
    doLast {
        println 'Your gradle file is READY'
    }
}
